type Props = {
    src: string;
    bg?: 'white';
    name?: string;
    size?: 's' | 'm' | 'l';
    alt?: string;
    className?: string;
};
export declare const Avatar: ({ src, bg, size, alt, className }: Props) => JSX.Element;
export {};
