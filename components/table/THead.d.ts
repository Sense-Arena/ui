import { PropsWithChildren } from 'react';
type Props = PropsWithChildren<{
    className?: string;
}>;
export declare const THead: ({ className, children, ...rest }: Props) => JSX.Element;
export {};
