import { disableSelectStyle, linkInsertion, redTitle, subtitleSection, vars } from './theme.css';

/**
 * production only
 */
export const themeVars = vars;
/**
 * production only
 */
export const themeStyles = {
  disableSelectStyle,
  linkInsertion,
  redTitle,
  subtitleSection,
};
